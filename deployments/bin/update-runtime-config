#!/usr/bin/env bash

source "$BASE_DIR/lib/common.inc.bash"
assert runtime-config "$RUNTIME_CONFG_DIR"

DEPL_DIR=$RUNTIME_CONFG_DIR # Shim: 'fetch_ubdms' is expecting this
fetch_ubdms

source "$RUNTIME_CONFG_DIR/conf/operations-layout.inc.bash"

DEPL_DIR=$RUNTIME_CONFG_DIR # Shim: 'build_operations_arguments' is expecting this
build_operations_arguments

RUNTIME_CONFG_FILE=$MAIN_DEPLOYMENT_FILE # Shim
bosh interpolate "$RUNTIME_CONFG_FILE" \
    "${operations_arguments[@]}" \
    > "$RUNTIME_CONFG_DIR/state/runtime-config.yml"

if [ ! -s "$RUNTIME_CONFG_DIR/state/runtime-config.yml" ]; then
    echo "Cloud config file is empty. ${BLUE}Skipping$RESET update."
    exit 0
fi
bosh update-runtime-config "$RUNTIME_CONFG_DIR/state/runtime-config.yml" "$@"
