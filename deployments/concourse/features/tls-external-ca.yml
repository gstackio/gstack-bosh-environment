---

- path: /variables?/name=atc_ca
  type: remove

# Note: we don't define the CA internally in this Concourse deployment
# anymore. Now, we have this CA be defined in CF deployment, and imported
# here instead.

# - path: /variables?/name=atc_ca
#   type: replace
#   value:
#     name: concourse-external-ca
#     type: certificate
#     options:
#       is_ca: true
#       common_name: Concourse external CA

- path: /variables?/name=atc_tls/options/ca
  type: replace
  value: concourse-external-ca
